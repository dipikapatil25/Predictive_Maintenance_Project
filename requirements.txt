# ============================================================
# Predictive Maintenance of Industrial Machinery
# AICTE-2026 Problem Statement #39
# requirements.txt
# ============================================================
# Install all dependencies:
#   pip install -r requirements.txt
# ============================================================

# ── Data science core ───────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
imbalanced-learn>=0.11.0      # SMOTE for class-imbalance handling
joblib>=1.3.0                 # Model serialisation (also bundled with sklearn)

# ── Visualisation ───────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.11.0                 # KDE plots (used by seaborn internally)

# ── Jupyter notebook ────────────────────────────────────────
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.25.0

# ── Flask REST API backend ──────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0             # Cross-origin support (Streamlit → Flask)

# ── Streamlit frontend ──────────────────────────────────────
streamlit>=1.28.0
requests>=2.31.0              # Streamlit HTTP calls to Flask API

# ── Report generation (DOCX — later step) ──────────────────
python-docx>=1.1.0
openpyxl>=3.1.0               # Excel support used by python-docx / pandas
